import { SearchSitterByNamePipe } from './search-sitter-by-name.pipe';

describe('SearchSitterByNamePipe', () => {
  it('create an instance', () => {
    const pipe = new SearchSitterByNamePipe();
    expect(pipe).toBeTruthy();
  });
});
